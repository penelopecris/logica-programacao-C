#include <stdio.h>
#include <string.h>

#define MAX_STRING_LENGTH 200
float num1, num2;
void numeros();

int main() {
  void numeros();
  

}
  void numeros() {

  printf("digite um número: ");
  scanf("%f", num1);
  printf("digite outro número, maior que o anterior: ");
  scanf("%f", num2);

   if (num1 > num2) {
    printf("Erro! O primeiro número deve ser menor que o segundo.\n");
   }
  }
